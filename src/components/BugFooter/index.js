import { Typography } from "@mui/material";
import React from "react";

const BugFooter = () => {
  return (
    <div>
      <Typography color={"error"}>this this bug footer section</Typography>
    </div>
  );
};

export default BugFooter;
